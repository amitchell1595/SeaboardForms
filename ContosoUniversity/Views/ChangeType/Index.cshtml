@model IEnumerable<ContosoUniversity.Models.ChangeType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChangeTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChangeTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChangeTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChangeTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChangeTypeID })
        </td>
    </tr>
}

</table>
